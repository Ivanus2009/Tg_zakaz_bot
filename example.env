# Ключ интеграции YTimes API
YT_API_KEY=your_api_key_here

# GUID торговой точки по умолчанию
YT_SHOP_GUID=your_shop_guid_here

# Токен Telegram бота (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Секрет для запросов бота к backend (POST /api/order-from-payment, GET /api/payment/pending).
# Сгенерировать: openssl rand -hex 32
BOT_INTERNAL_SECRET=

# Токен провайдера платежей Telegram (BotFather → Payments). Для инвойса в чат (старый способ).
PAYMENT_PROVIDER_TOKEN=

# ЮKassa: оплата картой прямо в Mini App (без инвойса в чат).
# Получить: https://yookassa.ru/ → Настройки → shopId и Секретный ключ.
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# URL backend для вызовов из бота (GET /api/payment/pending, POST /api/order-from-payment).
# На VPS в Docker: http://web:8000 . Локально: http://localhost:8000
BACKEND_URL=

# URL мини-приложения (обязательно HTTPS — Telegram не открывает http в боте).
# Без слэша в конце.
#
# --- Локальная разработка ---
# Чтобы тестировать кнопку «Открыть меню» из Telegram на телефоне: запустите
#   ngrok http 8000
# и подставьте выданный https://... URL сюда. Для разработки в браузере на
# localhost ngrok не обязателен.
#
# --- Продакшен / передача заказчику ---
# В проде ngrok не используется. Укажите постоянный URL, по которому доступно
# приложение: URL деплоя PaaS (например https://your-app.railway.app) или
# ваш домен (например https://cafe.example.com). После деплоя заказчик должен
# задать этот URL в своём .env (или он уже подставлен в настройках PaaS).
WEBAPP_URL=https://your-domain.com
