# Быстрый старт

## 1. Установка зависимостей

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## 2. Настройка .env

Скопируйте `example.env` в `.env` и заполните:

```bash
cp example.env .env
```

Отредактируйте `.env`:

- `YT_API_KEY` - ключ из YTimes (Настройки → Интеграции)
- `YT_SHOP_GUID` - GUID торговой точки (получить через `python scripts/get_shop_guid.py`)
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `WEBAPP_URL` - будет настроен после запуска ngrok

## 3. Запуск для тестирования

### Терминал 1: Веб-приложение

```bash
source .venv/bin/activate
uvicorn src.webapp.app:app --host 0.0.0.0 --port 8000
```

### Терминал 2: Ngrok (для HTTPS)

```bash
ngrok http 8000
```

Скопируйте HTTPS URL (например, `https://abc123.ngrok-free.app`) и обновите `.env`:

```
WEBAPP_URL=https://abc123.ngrok-free.app
```

### Терминал 3: Telegram-бот

```bash
source .venv/bin/activate
python src/bot/bot.py
```

## 4. Тестирование

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "🛒 Открыть меню"
4. Выберите напиток, размер, добавки
5. Оформите заказ

## Проверка меню

Проверить, что меню загружается:

```bash
source .venv/bin/activate
python scripts/list_coffee_items.py
```

Должна вывестись группа "Меню ( онлайн заказы )" с позициями.
