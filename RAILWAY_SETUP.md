# Настройка деплоя на Railway

## Шаг 1: Регистрация на Railway

1. Откройте [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Выберите **"Login with GitHub"**
4. Авторизуйтесь через GitHub

## Шаг 2: Создание проекта

1. После входа нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите репозиторий `Ivanus2009/Tg_zakaz_bot`
4. Railway автоматически определит Python проект и начнет деплой

## Шаг 3: Настройка переменных окружения

После создания проекта:

1. Откройте ваш проект в Railway
2. Перейдите в **"Variables"** (вкладка слева)
3. Добавьте следующие переменные:

```
TELEGRAM_BOT_TOKEN=8357146502:AAF9ZBFC3DUG64bpUNp-YGMg_JlDnXOtDHw
YT_API_KEY=ваш_ключ_из_ytimes
YT_SHOP_GUID=bdd03b7e-4fd9-4081-bfc0-959229d79044
```

⚠️ **Важно**: `WEBAPP_URL` Railway установит автоматически после деплоя веб-сервиса

## Шаг 4: Настройка сервисов

Railway определит два сервиса из `Procfile`:

- **web** - веб-приложение (мини-приложение)
- **bot** - Telegram-бот

### Настройка веб-сервиса:

1. Railway автоматически создаст сервис для `web`
2. После деплоя вы получите HTTPS URL (например: `https://your-app.railway.app`)
3. Скопируйте этот URL

### Настройка бота:

1. Railway создаст второй сервис для `bot`
2. Добавьте переменную окружения `WEBAPP_URL` в сервис бота:
   - Откройте сервис `bot`
   - Перейдите в **"Variables"**
   - Добавьте: `WEBAPP_URL=https://your-app.railway.app` (URL из веб-сервиса)

## Шаг 5: Обновление настроек бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Введите URL вашего веб-приложения (из Railway)
6. Нажмите **"Set Menu Button"**

## Шаг 6: Проверка работы

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "🛒 Открыть меню"
4. Должно открыться мини-приложение

## Мониторинг и логи

В Railway вы можете:

- Просматривать логи в реальном времени
- Видеть метрики использования
- Перезапускать сервисы
- Обновлять переменные окружения

## Обновление кода

После каждого `git push` в репозиторий Railway автоматически:

1. Обнаружит изменения
2. Пересоберет проект
3. Перезапустит сервисы

## Стоимость

- **Бесплатный тариф**: $5 кредитов в месяц
- Для этого проекта обычно хватает бесплатного тарифа
- Если превысите лимит, Railway уведомит вас

## Troubleshooting

### Бот не отвечает:

- Проверьте логи сервиса `bot` в Railway
- Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте, что сервис `bot` запущен

### Мини-приложение не открывается:

- Проверьте, что `WEBAPP_URL` правильный в переменных окружения бота
- Убедитесь, что сервис `web` запущен
- Проверьте логи сервиса `web`

### Ошибки при деплое:

- Проверьте, что все зависимости в `requirements.txt`
- Убедитесь, что `Procfile` правильный
- Проверьте логи сборки в Railway
