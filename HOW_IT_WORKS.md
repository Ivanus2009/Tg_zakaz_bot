# Как работает бот и программа

## Общая схема

```
Пользователь в Telegram
        │
        ▼
   Telegram-бот (aiogram)
   • /start — приветствие, кнопка «Открыть меню»
   • Открывает Mini App по WEBAPP_URL
        │
        ▼
   Mini App (FastAPI + React)
   • Загружает меню и добавки с бэкенда
   • Пользователь выбирает позиции, размеры, добавки → корзина → оформление
   • POST /api/order → бэкенд отправляет заказ в YTimes, сохраняет в БД, возвращает order_id
   • sendData() в бота → бот пишет пользователю «Заказ оформлен»
        │
        ▼
   Бэкенд (FastAPI)
   • /api/menu — меню из YTimes (только «Меню ( онлайн заказы )»)
   • /api/supplements — добавки из YTimes
   • /api/order — создаёт заказ в YTimes (касса), сохраняет в БД (для вебхука)
   • /api/webhook/order-status — приём статуса от YTimes (принят/отклонён кассиром), уведомление пользователя в Telegram
```

Два процесса (из `Procfile`):
- **web** — uvicorn (FastAPI): отдаёт Mini App и API.
- **bot** — python bot: long polling Telegram, регистрация пользователей в SQLite, приём данных из Mini App.

---

## 1. Telegram-бот

- **Стек:** aiogram 3, Python.
- **База:** SQLite в `data/bot.db` — пользователи и заказы (при создании заказа сохраняются ytimes_order_id, user_telegram_id, total, status для вебхука).
- **Команды:**
  - `/start` — приветствие, создание/обновление пользователя в БД, клавиатура с кнопкой «🛒 Открыть меню» (Web App).
  - `/menu` — та же кнопка «Открыть меню».
- **Данные из Mini App** (`handle_webapp_data`): приходит JSON с `action: "order_created"` (order_id, total, paid). Бот отвечает сообщением «Заказ оформлен, номер …, сумма …, оплата при получении/оплачено онлайн».

---

## 2. Mini App (веб-приложение внутри Telegram)

- **Стек:** React (Vite), TypeScript. Сборка — `frontend/dist`, раздаётся FastAPI при наличии папки.
- **URL:** задаётся в боте через `WEBAPP_URL` (тот же хост, что и FastAPI).
- **Страница:** SPA на React (экраны: меню, размер/количество, добавки, профиль, модальная корзина).

**Экраны:**
1. **Меню** — сетка карточек из `itemList` группы «Меню ( онлайн заказы )». У каждой позиции: название, «от X ₽». Клик → если есть размеры → экран размера, иначе если есть добавки → экран добавок, иначе — сразу в корзину.
2. **Размер и количество** — выбор типа (объём), счётчик количества, кнопка «Далее» → экран добавок.
3. **Добавки** — чекбоксы по категориям из `supplementCategoryToFreeCount`, кнопка «Добавить в корзину».
4. **Корзина** — модальная панель снизу: список позиций, итог, «Оформить заказ».
5. **Профиль** — имя из Telegram, история заказов из `localStorage` (не с сервера).

**Логика заказа:**
- Корзина — массив в JS: `{ menuItemGuid, menuTypeGuid, supplementList, priceWithDiscount, quantity, name, typeName, price }`.
- При «Оформить заказ» — подтверждение через `tg.showConfirm`, затем POST `/api/order` с `type: 'TOGO'`, `items`, `client`, `paidValue: 0`, `telegramUserId` (из Telegram), `comment` (опционально). Ответ: `order_id` (guid заказа в YTimes), `total`, `status`.
- После успеха: заказ добавляется в `orders` и в `localStorage`, вызывается `tg.sendData({ action: 'order_created', order_id, total, paid: false })`, корзина очищается, показ алерта.

**Меню и добавки:** только из API (YTimes), фильтр по названию группы «Меню ( онлайн заказы )» делается на бэкенде в `/api/menu`.

---

## 3. Бэкенд (FastAPI)

- **Клиент YTimes** создаётся при старте из `.env` (`YT_API_KEY`, опционально `YT_SHOP_GUID`).
- **Эндпоинты:**
  - `GET /` — отдача React SPA из `frontend/dist` (если папка есть), иначе старый `index.html` из `templates/`.
  - `GET /health` — health-check для PaaS.
  - `GET /api/menu` — `ytimes.get_menu_items()` → из списка групп выбирается группа с именем «Меню ( онлайн заказы )» → в ответе только она.
  - `GET /api/supplements` — список добавок из YTimes.
  - `POST /api/order` — приём позиций, создание заказа в YTimes (POST /ex/order/save), сохранение в БД (для вебхука), ответ с `order_id` (guid), `status`, `total`.
  - `POST /api/webhook/order-status` — приём от YTimes статуса заказа (ACCEPTED/CANCELLED), обновление БД и отправка уведомления пользователю в Telegram; ответ 200 OK.

---

## 4. YTimes

- Используется для: списка торговых точек, групп меню, позиций и добавок, а также **создания заказа на кассе** (POST /ex/order/save).
- Меню в боте — только группа «Меню ( онлайн заказы )».
- Заказ создаётся в YTimes при оформлении из Mini App; на точке кассир принимает или отклоняет заказ. Статус приходит на наш вебхук, пользователь получает уведомление в Telegram.

---

## 5. Что не сделано / ограничения

- Оплата только «при получении» (`paidValue: 0` / null в API); онлайн-оплата не интегрирована.
- В меню отображаются только `itemList`; `goodsList` из той же группы в UI не выводятся (при необходимости можно добавить).

---

## 6. Что нужно, чтобы приложение открывалось в боте

Чтобы по кнопке «🛒 Открыть меню» в Telegram действительно открывалось ваше приложение, нужно:

1. **Запущены оба процесса.** Одна команда из корня проекта:
   ```bash
   ./start.sh
   ```
   Скрипт освобождает порт 8000 и запускает веб (uvicorn) и бота. Остановка — Ctrl+C.
   - **web** — сервер (uvicorn), который отдаёт страницу и API.
   - **bot** — бот (python), который показывает кнопку и обрабатывает данные из Mini App.

2. **В `.env` задан `WEBAPP_URL`** — полный **HTTPS**-адрес, по которому доступен ваш web-сервер (без слэша в конце).  
   Telegram **не открывает** Mini App по `http://` или `localhost` с телефона.

   - **Локальная разработка (опционально ngrok):** чтобы тестировать кнопку «Открыть меню» прямо из Telegram на телефоне, поднимите туннель (например, `ngrok http 8000`) и подставьте выданный `https://...` в `WEBAPP_URL`. Для разработки в браузере на localhost ngrok не нужен.
   - **Продакшен:** `WEBAPP_URL` всегда равен **постоянному URL деплоя** (PaaS или ваш домен). **В проде ngrok не используется** — только реальный хост (Railway, Render, VPS с доменом и т.д.). После деплоя укажите в `.env` этот URL (например `https://your-app.railway.app` или `https://cafe.example.com`).

3. **На сервере после деплоя:** чтобы отдавалась React-версия, на сборке нужно выполнить `npm run build` в `frontend/` (чтобы появилась папка `frontend/dist`). Иначе FastAPI отдаст старую версию из `templates/index.html`.

Если всё настроено, в боте по «Открыть меню» откроется ваше приложение (React или старый вариант — в зависимости от пункта 3).

### 6.1. Деплой в прод и передача заказчику

**Вариант A: PaaS (Railway, Render, Fly.io)**

- Создайте проект с **двумя сервисами**: web (uvicorn) и bot (worker).
- **Web:** в build-фазе выполните `npm run build` в `frontend/`, затем запуск `uvicorn src.webapp.app:app --host 0.0.0.0 --port $PORT`. В переменных окружения задайте `WEBAPP_URL` = публичный URL этого же web-сервиса (его выдаёт PaaS после деплоя), а также `TELEGRAM_BOT_TOKEN`, `YT_API_KEY`, `YT_SHOP_GUID`.
- **Bot:** отдельный сервис/воркер без HTTP, команда запуска `python src/bot/bot.py`. Те же переменные окружения; `WEBAPP_URL` должен совпадать с URL web-сервиса.
- **Передача заказчику:** разверните под его аккаунтом или выдайте доступ в панель. Инструкция: заполнить токен бота и ключи YTimes; `WEBAPP_URL` после первого деплоя уже подставлен (или задать свой домен в настройках PaaS).

**Вариант B: VPS + свой домен (Docker)**

- Сервер с публичным IP, домен с A-записью на этот IP. HTTPS через reverse proxy (nginx/Caddy) и Let's Encrypt.
- В корне проекта: `docker compose up -d` — поднимаются сервисы **web** и **bot** с общим `.env`. Сборка фронта входит в образ web.
- Заказчику: скопировать репозиторий, заполнить `.env` (в т.ч. `WEBAPP_URL=https://их-домен.ru`), при необходимости один раз настроить nginx/caddy и SSL на хосте. Подробнее — раздел «Запуск через Docker» ниже в документе.

---

## 7. Разработка и сборка Mini App (React)

- **Локальная разработка:** в одном терминале `uvicorn src.webapp.app:app --reload --port 8000`, в другом `cd frontend && npm install && npm run dev`. Открыть http://localhost:2000 — Vite проксирует `/api` на бэкенд.
- **Сборка для продакшена:** `cd frontend && npm run build`. После этого FastAPI при старте отдаёт приложение из `frontend/dist`.
- **Деплой:** перед запуском web-процесса нужно выполнить сборку фронтенда (например, в build-фазе на Railway добавить шаг с Node.js и `npm run build` в `frontend/`).

---

## 8. Ошибка ngrok 3200 (ERR_NGROK_3200)

Если в Telegram при нажатии «Открыть меню» появляется **ошибка 3200** — это **ngrok**: туннель не найден или выключен.

**Что делать:**

1. **Запустить ngrok до бота.** В отдельном терминале:
   ```bash
   ngrok http 8000
   ```
   (Порт 8000 — тот, на котором крутится uvicorn.)

2. **Скопировать текущий HTTPS-адрес** из вывода ngrok (например `https://a1b2c3.ngrok-free.app`) и прописать в `.env`:
   ```
   WEBAPP_URL=https://a1b2c3.ngrok-free.app
   ```
   Без слэша в конце.

3. **Перезапустить бота** (чтобы он подхватил новый `WEBAPP_URL`).

4. **Важно:** в бесплатном ngrok URL **меняется при каждом новом запуске** ngrok. После каждого перезапуска `ngrok http 8000` нужно обновить `WEBAPP_URL` в `.env` и снова перезапустить бота.

5. Чтобы URL не менялся при перезапусках, можно использовать [endpoint pooling](https://ngrok.com/docs/errors/err_ngrok_3200):  
   `ngrok http 8000 --pooling-enabled=true` (по возможности в вашем тарифе ngrok).

---

## 9. Запуск через Docker (VPS / передача заказчику)

Для деплоя на свой сервер или передачи заказчику «под ключ» можно использовать Docker Compose.

1. **Требования:** на сервере установлены Docker и Docker Compose. Домен направлен на IP сервера (A-запись). HTTPS настраивается на хосте (nginx или Caddy + Let's Encrypt), проксирование на порт 8000 контейнера `web`.

2. **Подготовка:** скопируйте репозиторий на сервер. Создайте `.env` из `example.env`, заполните:
   - `TELEGRAM_BOT_TOKEN`, `YT_API_KEY`, `YT_SHOP_GUID`
   - `WEBAPP_URL=https://ваш-домен.ru` (без слэша в конце) — тот URL, по которому пользователи будут открывать приложение (после настройки SSL).

3. **Запуск:** из корня проекта выполните:
   ```bash
   docker compose up -d
   ```
   Поднимаются два контейнера: **web** (FastAPI + собранный фронт) и **bot** (Telegram long polling). Оба читают один `.env`.

4. **Остановка:** `docker compose down`.

Подробности по настройке nginx/Caddy и выпуску сертификата см. в [README_DOCKER.md](README_DOCKER.md) в корне проекта.

---

## 10. Локальная тестовая среда (без VPS и домена)

Чтобы тестировать UI и логику заказов (в т.ч. появление заказа на кассе и уведомления от кассира), достаточно поднять приложение локально и выставить его в интернет через ngrok.

1. **Запуск приложения.** Из корня проекта:
   ```bash
   ./start.sh
   ```
   Запустятся веб (uvicorn на порту 8000) и бот.

2. **Туннель ngrok.** В отдельном терминале:
   ```bash
   ngrok http 8000
   ```
   Скопируйте выданный **https://...** URL (например `https://abc123.ngrok-free.app`).

3. **Настройка .env.** В `.env` задайте:
   - `WEBAPP_URL=https://ваш-ngrok-хост` (без слэша в конце) — тот же URL из шага 2.
   - `TELEGRAM_BOT_TOKEN`, `YT_API_KEY`, `YT_SHOP_GUID` — как для продакшена/теста.
   После смены `WEBAPP_URL` перезапустите бота (остановите и снова запустите `./start.sh` или только процесс бота).

4. **Вебхук YTimes.** Чтобы получать статусы «принят/отклонён» и слать уведомления пользователю в Telegram:
   - В личном кабинете YTimes откройте настройки интеграции (раздел **Webhook API**).
   - В поле «Статус заказа, созданного по API» укажите адрес:  
     `https://ваш-ngrok-хост/api/webhook/order-status`  
     (тот же хост, что и в `WEBAPP_URL`, путь `/api/webhook/order-status`).
   - Сохраните. При каждом новом запуске ngrok URL меняется — обновите и `WEBAPP_URL`, и адрес вебхука в YTimes.

5. **Проверка.** Откройте бота в Telegram, нажмите «Открыть меню», соберите корзину и оформите заказ. Заказ должен появиться в YTimes на кассе. При принятии или отклонении заказа кассиром на ваш сервер придёт запрос на вебхук, пользователю в Telegram уйдёт сообщение «Заказ принят» или «Заказ отклонён: …».

Без VPS и домена для теста достаточно ngrok; в проде вместо ngrok используются постоянный домен или URL PaaS для `WEBAPP_URL` и для адреса вебхука в YTimes.
